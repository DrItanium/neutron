Neutron is a version of electron but with graphical additions thanks to
plan9port integration. It uses the default electron runtime's libelectron.a in
addition to its own stuff to create the neutron program
